# AeroBeat Web Video Testbed

This hidden testbed owns repo-local tests, demos, scenes, debug data, browser validation, and generated local dependency symlinks.

Create `.testbed/node_modules/@aerobeat/web-video` as a local symlink to `../../../src` when validating package-imported demos. Add sibling `@aerobeat/web-*` symlinks only for declared public package dependencies.
